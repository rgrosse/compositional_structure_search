# experiment directories
RESULTS_PATH = '/path/to/results'
CACHE_PATH = '/path/to/cached'
REPORT_PATH = '/path/to/reports'

# 'single_process' to run in a single process, 'parallel' to use GNU Parallel
# (single_process not done yet)
SCHEDULER = 'parallel'

# additional options for GNU Parallel
DEFAULT_NUM_JOBS = 2
JOBS_PATH = '/path/to/job_info'
